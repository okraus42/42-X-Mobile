// theme/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  // primary accent (temperature, highlights)
  static const Color primary = Color(0xFF4DA3FF);

  // secondary accent (icons, highlights)
  static const Color accent = Color(0xFFFFB74D);

  // text overlay color (for dark background readability)
  static const Color textLight = Colors.white;

  // muted text (region/country)
  static const Color mutedText = Colors.white70;
}